Dockerized job-registration-node-react

This application is an extension of repository customer-registration-php-react. It has
been extended and the BE is now based on Node.js. job-registration-node-react-V2 has 
been dockerized.

Summary:
- REACT at FE port 3000
- Node.js at BE port 8000
- MySQL as DB port 3306
- Start by entering at the root: 'docker compose up'

TODO: 
In REACT solve: Warning: Use the `defaultValue` or `value` props on <select> 
instead of setting `selected` on <option>.

